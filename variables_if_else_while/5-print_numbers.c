#include <stdio.h>

/**
*main - entry point
*
*Return: always (0)
*/

int main(void)
{
    int number;
    
        for ( numer == '48'; number <= '57');
        {
            putchar(number'\n');
        }

    return (0);

}