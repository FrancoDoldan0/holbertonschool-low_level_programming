#include <stdio.h>

/**
*main - entry point
*
*Return: always (0)
**/

int main(void)
{
    int number;
    
        for (number = 48; number <= 57; number++)
        {
            putchar (number);
        }

    putchar('\n');
    return (0);

}